package Purchase;

import java.sql.Connection;

public class Purchase {
    public static void start(Connection conn) {
        // ask category
            // if grocery ask which grocery type or create new

        // else add to receipt item table
    }
}
